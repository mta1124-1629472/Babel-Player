# Babel Player — GitHub Pages (Jekyll)

Static marketing site for **https://babelworks.github.io/Babel-Player/**

- Edit files on this branch (`site`) and push to update the live site (workflow runs from `main`’s `.github/workflows/jekyll-gh-pages.yml`).
- Application source code lives on branch **`main`** only.
