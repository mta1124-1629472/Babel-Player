name: Release Win x64

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  publish-win-x64:
    name: Publish Windows x64 Self-Contained Release
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore Babel-Player.sln -r win-x64

      - name: Publish self-contained build
        run: >
          dotnet publish BabelPlayer.csproj
          --configuration Release
          -r win-x64
          --self-contained true
          -p:ApplicationIcon=babelplayer.ico
          -o ./publish

      - name: Verify publish output
        shell: pwsh
        run: |
          $exePath = "./publish/BabelPlayer.exe"
          if (-not (Test-Path $exePath)) {
            throw "Expected executable not found at $exePath"
          }

      - name: Package self-contained publish output
        id: package
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $publishDir = "${{ github.workspace }}/publish"

          if (-not (Test-Path $publishDir)) {
            throw "Publish output not found at $publishDir"
          }

          New-Item -ItemType Directory -Path "dist" -Force | Out-Null
          $zipPath = "dist/Babel-Player-$tag-win-x64-self-contained.zip"

          Compress-Archive -Path "$publishDir/*" -DestinationPath $zipPath -Force

          "zip_path=$zipPath" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: babel-player-${{ github.ref_name }}-win-x64-self-contained
          path: ${{ steps.package.outputs.zip_path }}
          if-no-files-found: error
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.package.outputs.zip_path }}
          generate_release_notes: true
